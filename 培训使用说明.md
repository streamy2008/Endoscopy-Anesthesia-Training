# 内镜麻醉无线监护文书系统 · 现场培训交互页面

> 交付说明与带教手册 ｜ 版本 V1.0 ｜ 2026-09
> 素材来源：`内镜麻醉无线监护文书系统操作流程.pptx`（21 页，2026-09）

---

## 一、这是什么

把原课件 21 页的线性讲解，重做成**可点、可练、可考**的现场培训页面：

| 原课件 | 本页面 |
|---|---|
| 21 页逐页翻讲 | 4 大环节 **14 步操作卡**（要点 + 易错点 + 原截图放大） |
| 口头讲"扫码绑定" | **扫码绑定与转床模拟器**：11 个节点按序点亮，做错即时纠偏 |
| 无练习 | **流程排序挑战**（10 步）+ **现场情境判断题**（8 题） |
| 无考核 | **结业考核 10 题**（含多选、解析、成绩单打印） |
| 无留存 | **一页速查卡**，A4 打印贴检查间/复苏室/门诊工位 |

技术上是**零依赖单页应用**：无 CDN、无构建、无联网，双击即开，适合培训室断网环境。

---

## 二、交付物清单

```
20260914 京东方医院内镜培训交互式页面/
├── index.html                    ← 主交付（双击打开）
├── assets/
│   ├── app.css                   设计系统（mintlify 风格）+ 四角色配色 + 动效 + 打印样式
│   ├── data.js                   ★ 全部培训内容 + 热点坐标表（改内容只改这一个文件）
│   ├── app.js                    交互逻辑（分步图片演示/导航/灯箱/模拟器/排序/判断/考核/讲师模式）
│   └── img/                      16 张原课件截图（已按步骤重命名）
├── 京东方内镜培训-单文件版.html    ← 打包版：CSS/JS/图片全内联，17.8 MB，便于 U 盘/微信传递
├── tools/
│   ├── build-single.mjs          多文件版 → 单文件版 打包脚本
│   ├── detect-annotations.py     OpenCV 自动检出截图里的红色标注框（热点坐标的自动来源）
│   ├── annotations.json          上一步的检出结果（坐标校准用，不参与运行）
│   └── verify.py                 交付前自查（Playwright 实机跑两种产物 + 全部交互断言）
└── 培训使用说明.md                ← 本文件
```

**两种用法**：
- 日常维护/要改内容 → 用 `index.html`（结构清晰，改 `assets/data.js` 即可）
- 拷到培训室电脑 / 发给同事 → 用 `京东方内镜培训-单文件版.html`（一个文件搞定，无需 assets 目录）

---

## 三、现场怎么用（建议 60 分钟议程）

| 时间 | 环节 | 页面模块 | 讲师动作 |
|---|---|---|---|
| 0–3′ | 开场 | 首屏 | 点「讲师模式」，用首屏业务流程时间轴说明**五个节点全自动生成** |
| 3–8′ | 认设备 | 01 系统概述 | 点硬件图编号，逐个讲中继器"成套绑定"与患者条码 |
| 8–15′ | 串流程 | 02 全流程地图 | 点「▶ 播放全流程巡演」，14 步自动走一遍，先建立整体感 |
| 15–40′ | 分角色实训 | 03 角色实训 | 按 Ⅰ→Ⅳ 切角色 Tab，重点讲每张卡右下角的**易错点**；截图点开放大看细节 |
| 40–48′ | 上台实操 | 04 扫码模拟器 | 抽 2–3 位学员上台点，顺序错了页面会直接告诉他该做什么 |
| 48–55′ | 抢答 | 05 排序 + 06 判断题 | 分组接力排 10 步；判断题用"举手选对/错"再点开解析 |
| 55–60′ | 考核 | 07 结业考核 | 各自扫码/在本机做 10 题，交卷后「打印成绩单」留存 |

**讲师模式**（顶栏「讲师模式」或按 `P`）：隐藏侧栏、内容加宽、底部出现**计时器 + 章节进度 + 上一章/下一章**，适合投影。

**投影可读性**：顶栏 `A / A+ / A++` 三档字号，会议室最后一排也能看清。

**快捷键**：`←` `→` / `空格` 章节翻页 ｜ `P` 讲师模式 ｜ `F` 全屏 ｜ `Esc` 关闭浮层 ｜ 截图滚轮缩放

**进度留存**：每步「标记已掌握」与考核成绩存浏览器本地（localStorage），下次打开还在；换电脑不同步。

---

## 四、八个模块各自怎么用

1. **首屏**：右侧"麻醉门诊业务流程"示意卡 = 真实文书里那 5 行时间轴的同构展示，用来讲"为什么要扫码"。
2. **01 系统概述**：四大能力卡 + **设备交互图**（点图中编号或右侧列表，显示该设备在流程中的角色与两条操作提示）。
3. **02 全流程地图**：四角色泳道，点任意步骤跳到对应操作卡；「播放全流程巡演」自动高亮走一遍。
4. **03 角色实训（分步图片演示）**：14 张操作卡，每张都是**「图上一步步走」的演示**——
   - 点「下一步 →」：聚光灯移到这一步该点的按钮/字段上，右上角计数变成「第 n / N 步」，下方同步换成该步文字；
   - 点右侧操作项列表的任意一条 → 图片演示直接跳到那一步（反向联动）；
   - 点小圆点直接跳步；「▶ 自动演示」每 1.8 秒走一步，适合讲师串讲；
   - 焦点在演示里时 `←` `→` `Home` `End` 控制走图（此时不翻章节）；
   - 点图片本身仍可放大（滚轮缩放、拖动看细节）。
   详见第七节「交互式图片演示」。
5. **04 扫码模拟器**：左侧设备台 + 模拟床旁屏，右侧实时生成文书时间轴。11 个动作必须按序做；做错会提示"现在应该操作什么"。点「自动演示」可让页面自己走完，适合串讲。
6. **05 流程排序挑战**：拖动排序（触屏用 ↑ ↓ 按钮），提交后逐条标对错并给出"应为第 N 步"。
7. **06 现场情境判断题**：8 个来自现场的"差不多就行"念头，选完立即给依据。
8. **07 结业考核**：10 题（含 3 道多选），交卷后显示分数 + 等级 + 错题清单 + 逐题解析，可打印成绩单。
9. **08 一页速查卡**：四角色各一张 + 全流程口诀，点「打印速查卡」只输出本区域，A4 一页。

---

## 五、⚠️ 上培训前请确认三件事

1. **截图里的医院名称**：原课件截图左上角显示的是「**首都医科大学附属北京佑安医院 · 无线监护文书系统 v1.2.7**」。若本次培训对象是**京东方医院**，建议：
   - 方案 A（最快）：课堂开场说明"截图为兄弟医院界面，功能完全一致"；
   - 方案 B：在京东方现场按同一视角重拍 16 张截图，替换 `assets/img/` 下同名文件即可，页面无需改动。
2. **版本号**：课件中系统版本为 v1.2.7 / 登录页 v1.9.2。页面文案写的是"示例 v1.9.2，以现场实际为准"，如与现场不一致请告知我统一改。
3. **初始密码**：页面写的是"首次绑定密码 123456"，请与信息科确认是否仍为默认值；若已改，告诉我改 `assets/data.js` 里 S02 那一步。

---

## 六、怎么改内容

所有文字、图注、题目、模拟脚本都在 **`assets/data.js`** 一个文件里：

| 想改什么 | 改哪个数组 |
|---|---|
| 系统名称/版本/首屏文案/四个数字 | `META` |
| 四大能力卡 | `CAPS` |
| 设备交互图（名称、简介、图中坐标 `x/y` 百分比） | `DEVICES` |
| 四环节名称/地点/本环节最易错 | `ROLES` |
| 14 步操作卡（标题、标签、截图、编号动作、易错点） | `STEPS` |
| **分步图片演示的热点坐标**（每步每个操作打在图上哪个位置） | `HOT` |
| 模拟器的设备台与 11 个动作 | `SIM_TOOLS` / `SIM_STEPS` |
| 排序题 10 条 | `SORT_ITEMS` |
| 判断题 8 条 | `JUDGE` |
| 考核 10 题 | `QUIZ` |

改完刷新浏览器即可，无需构建。改完**单文件版要重新打包**：

```bash
node tools/build-single.mjs
```

**交付前自查**（Playwright 实机跑两种产物，断言 25+ 项：渲染数量、**14 个分步演示 / 61 步热点**、模拟器 11 步、灯箱、讲师模式、考核满分、首屏动效终态、泳道进度填充、无 JS 错误、无断图、无横向溢出）：

```bash
/Users/sean/.agents/skills/cad-viewer/scripts/viewer/.venv/bin/python tools/verify.py
```

---

## 七、交互式图片演示：文字操作项 → 图上一步步走

角色实训的 14 张操作卡，左栏不再是"一张静态图 + 一串文字"，而是**可以一步步往下走的图片演示**。

**怎么用**

| 操作 | 结果 |
|---|---|
| 点「下一步 →」 | 聚光灯移到这一步该点的**按钮 / 字段 / 区域**上，右上角计数变「第 n / N 步」，下方文字同步换成该步 |
| 点右侧操作项列表任意一条 | 图片演示直接跳到那一步（**反向联动**），当前项高亮 |
| 点小圆点 | 直接跳到第 n 步 |
| 「▶ 自动演示」 | 每 1.8 秒自动走一步，讲师串讲用；到末步自动停 |
| 焦点在演示里按 `←` `→` `Home` `End` | 走图（此时**不**触发章节翻页） |
| 点图片本身 | 仍可放大：滚轮缩放、按住拖动看系统里的小字 |

**热点怎么定的（不是估的）**

1. `tools/detect-annotations.py`（OpenCV）先把厂商在截图里画的**红色标注**（红框/红字/红箭头）按颜色阈值检出成矩形，输出 `tools/annotations.json`；
2. 给每张图叠 **10% 网格标尺**，逐张人工读坐标并和检出框互相印证；
3. 结果写进 `assets/data.js` 的 **`HOT`** 表 —— 与 `STEPS` 解耦，键＝步骤 id，值＝与 `ops` 逐条对齐的数组。

当前覆盖：**14 步 / 61 个操作项，其中 59 个有精确定位热点，2 个诚实降级**（那两步的动作发生在文书系统内部，截图里没有对应元素 → 整图轻压暗 + 文字说明，不硬凑一个假框）。

**要调坐标**：改 `HOT` 里对应项的 `x/y/w/h`（0..1 归一化，相对该张截图），刷新即可；`x` 是从左、`y` 是从上、`w/h` 是宽高。

---

## 八、动效系统：让"流程在流动"被看见

页面上的动画只表达一件事——**流程在推进**。没有装饰性动画。

| 位置 | 动效 | 传达的信息 |
|---|---|---|
| 首屏业务流程时间轴 | 一条薄荷色轨道自上而下点亮（2.6s），游标点随轨道下移，5 个节点依次落位、勾选轻弹一次，全部到位后卡片收口一次薄荷描边 | 这 5 个节点是**顺着时间自动长出来的**，不是人工填的 |
| 首屏时间轴 · 重播 | 卡片右上角「↻ 重播」；进入视口自动播一次（4 秒内不重复播，避免来回滚动闪烁） | 讲师可随时重播给学员看 |
| 全流程地图 · 巡演 | 每完成一步，该角色泳道顶部的细条按比例填充；当前步骤带呼吸光晕 | 四个环节各自的**完成度** |
| 分步图片演示 | 聚光灯在步与步之间**滑动**（0.5s easeOutQuint），不是闪一下；跨图切换先淡出再换源 | 视线被带着走到下一个目标上 |
| 角色实训卡片 | 滚动进入时轻微上浮（14px / 550ms），同组卡片错峰 60–90ms 依次出现；步骤编号落位时有一次环形扩散 | 视线跟着内容走，不用找 |
| 扫码模拟器 · 时间轴 | 只有**新落位的那一行**做进场动画（不是整列重播）；计数徽标弹一下 | 新节点是"刚发生的" |
| 扫码模拟器 · 床旁屏 | 每次相位切换（绑定/文书/麻醉中/复苏…）屏上扫过一道薄荷光 | 数据流进了文书系统 |
| 扫码模拟器 · 波形 | ECG / SpO₂ 波形虚线缓慢流动 | 生命体征是**实时**的 |
| 考核 / 判断题 | 选项反馈轻微缩放一次、解析淡入、分数从下滑入 | 反馈即时且明确 |

**动效纪律**：时长 180ms（微）· 500ms（进场/聚光灯移动）· 2.6s（整条流程走完）；缓动统一 `cubic-bezier(.22,1,.36,1)`；**不做弹跳、不做视差、不做无限循环的干扰动画**（唯一循环的是波形流动与当前步骤呼吸光，都极弱）。

**无障碍与兜底**（都已实测）：
1. 系统开启「减弱动态效果」时，全部动画降为 1ms，首屏时间轴直接呈现**最终态**（5 个节点全亮），聚光灯瞬移不滑动，不影响理解。
2. 滚动揭示的 `.reveal` 标记**由 JS 添加** —— 万一 JS 未执行，元素本来就是可见的，不会白屏；另有 2.5s 兜底全部显形。
3. 所有动画元素在无动画时的状态＝动画结束态，且已写入 `tools/verify.py` 断言（首屏 5/5 行可见、`is-done` 收口态、仅最后一行带 `is-new`、泳道进度可填充可归零、聚光灯点击后位移、无热点步骤走 `is-plain` 降级）。

---

## 九、设计说明（可跳过）

- 视觉基线取自 `awesome-design-md` 技能库的 **mintlify** 风格：Inter 系 UI 字体 + 等宽元数据、发丝线边框、12px 卡片圆角、胶囊按钮、薄荷绿作唯一强调色。
- 在 mintlify 调色板内为四个环节分配色彩：门诊医生＝蓝 `#3772cf`、信息核对＝薄荷 `#00a882`、麻醉医生＝琥珀 `#c37d0d`、复苏护士＝橙 `#f55a3c`。**未引入调色板外的新颜色。**
- 中文优先使用系统字体（PingFang SC / 微软雅黑），不加载任何网络字体，保证断网可用。
- 响应式：≥1080px 三栏（侧栏／正文／内容）；<1080px 侧栏抽屉化；<620px 顶栏瘦身，手机可自学。
- 打印：`@media print` 只输出速查卡或成绩单，其余模块隐藏。

---

## 十、发布与线上地址

| | |
|---|---|
| 🌐 在线访问 | <https://boe-endoscopy-training.netlify.app> |
| 📦 代码仓库 | <https://github.com/streamy2008/Endoscopy-Anesthesia-Training> |
| 🚀 重新发布 | `tools/deploy.sh`（打包 → 发布 → 自动验收一条龙） |

**三条安全门禁**（都已生效，实测线上 404 验证过）：
1. **截图脱敏**：`tools/mask-pii.py` 对 13 张图共 23 处做处理 —— 姓名/患者ID/电话/证件号用像素化，
   **二维码与腕带条码用实心遮盖**（9px 像素块 ≈ 二维码模块尺寸，只做像素化图案会原样存活，仍可扫）。
   未脱敏原件留存于 `assets/img-original/`，已进 `.gitignore`。
2. **发布目录白名单**：`tools/build-dist.mjs` 只复制 `index.html` + `assets/`，并遍历 `dist/` 自检，
   出现任何非白名单文件立即中止。**绝不把项目根当发布目录**（根下有未脱敏原件与厂商课件）。
3. **不索引**：站点带 `X-Robots-Tag: noindex` + `robots.txt`。

`netlify.toml` 里 `publish = "dist"`；`tools/deploy.sh` 会重新打包、发布并 curl 线上验收（HTTP 码 / 标题 / 安全头）。
